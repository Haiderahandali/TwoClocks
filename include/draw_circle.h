#ifndef DRAW_CIRCLE_H
#define DRAW_CIRCLE_H
#include "SDL.h"

void DrawCircle(int Cx, int Cy, int radius);
void DrawPixel(int x, int y);
void DrawDisk(int x, int y, int radius);
static unsigned int CIRCLE_COLOR = 0X00000000;
#endif
